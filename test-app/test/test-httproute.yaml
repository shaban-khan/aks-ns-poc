apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nginx-route
  namespace: test
spec:
  parentRefs:
    - name: gateway
      namespace: envoy-gateway
  hostnames:
    - test.skdemo.online
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: nginx-service-test
          port: 80